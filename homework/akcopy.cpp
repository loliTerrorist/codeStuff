#include <bits/stdc++.h>
#define frabbit(i, nRabbit) for(int i=0;i<nRabbit;i++)
#define ftiger(i, nTiger) for(int i=0;i<nTiger;i++)
using namespace std;
    int nRabbit = 0;
vector<vector<set<int> > >  sset(4, vector<set<int> >(3));
void t(){
    frabbit(i, nRabbit){cout<<"hi";}
    cout<<sset[0][0].empty()<<endl;
}
int main(){
    frabbit(i, nRabbit){}
    t();
}

